info:
  name: Positions
  type: http
  seq: 2

http:
  method: GET
  url: https://api.rise.trade/v1/positions?account={{wallet}}
  params:
    - name: account
      value: "{{wallet}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
