info:
  name: Open Orders
  type: http
  seq: 3

http:
  method: GET
  url: https://api.rise.trade/v1/orders/open?account={{wallet}}
  params:
    - name: account
      value: "{{wallet}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
