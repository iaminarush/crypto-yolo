info:
  name: Cross margin balance
  type: http
  seq: 4

http:
  method: GET
  url: https://api.rise.trade/v1/account/cross-margin-balance?account={{wallet}}
  params:
    - name: account
      value: "{{wallet}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
